{
  "name": "invoice-web-v2",
  "private": true,
  "packageManager": "bun@1.3.7",
  "workspaces": [
    "client",
    "server",
    "marketing"
  ],
  "overrides": {
    "esbuild": ">=0.25.4"
  },
  "scripts": {
    "dev:client": "bun --cwd client dev",
    "dev:server": "bun --cwd server dev",
    "dev:marketing": "bun --cwd marketing dev",
    "build": "bun --cwd client build",
    "build:marketing": "bun --cwd marketing build",
    "lint": "bun --cwd client lint",
    "typecheck": "bun --cwd client typecheck && bun --cwd server tsc --noEmit && bun --cwd marketing typecheck",
    "test": "bun --cwd client test:smoke"
  }
}
